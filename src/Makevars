LIBDIR = rust_lib/target/release
STATLIB = $(LIBDIR)/librust_lib.a
PKG_LIBS = -L$(LIBDIR) -lrust_lib -lresolv

all: clean

$(SHLIB): $(STATLIB)

$(STATLIB):
	cargo build --release --manifest-path=rust_lib/Cargo.toml

clean:
	rm -Rf $(SHLIB) $(STATLIB) $(OBJECTS) rust_lib/target
